#!/bin/bash

##
# Volume can be anything between 1 to 10.
# First parameter must be the language. Ej: es, en, it
# Second parameter must be the text to say. Ej: Hola a todos
##
/usr/bin/gst-launch-0.10 playbin2 volume=10 uri="http://translate.google.com/translate_tts?tl=`echo $1`&q=`echo $2`"
